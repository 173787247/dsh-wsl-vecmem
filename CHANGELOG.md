# Changelog

## 0.2.0

- `workspace` string on `vecmem_add` / `vecmem_search` / `vecmem_clear` (stored and filtered as `meta.workspace`).
- New `vecmem_clear` — clear all, or only one workspace namespace.

## 0.1.0

- `vecmem_status`, `vecmem_add`, `vecmem_search` (Ollama embeddings + JSON store).
